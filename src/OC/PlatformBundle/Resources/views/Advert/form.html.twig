{# src/OC/PlatformBundle/Resources/views/Advert/form.html.twig #}


{# On laisse vide la vue pour l'instant, on la comblera plus tard
   lorsqu'on saura afficher un formulaire. #}
<div class="form-group">
	<label for="inputText" class="control-label">Titre</label>
	<input type="text" name="titre2" id="titre2" class="form-control" value="{{ advert.title }}">
</div>

<div class="form-group">
	<label for="textArea" class="control-label">Descriptif</label>
	<textarea class="form-control vertical" rows="3" name="descritpif" id="descritpif"  value="{{ advert.content }}">{{ advert.content }}</textarea>

</div>